@model KuponApp.Models.Coupon

@{
    ViewData["Title"] = "Kuponunuz Hazır!";
}

<div class="text-center">
    <h1 class="display-4 text-success">🎉 Tebrikler @Model.User.FirstName!</h1>
    <p class="lead">Özel indirim kuponunuz hazır!</p>
</div>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card border-success">
            <div class="card-header bg-success text-white text-center">
                <h3>İndirim Kuponu</h3>
            </div>
            <div class="card-body text-center">
                <h2 class="display-5 text-primary mb-4">@Model.CouponCode</h2>
                <h3 class="text-success mb-4">@Model.DiscountAmount.ToString("C") İndirim!</h3>
                
                <div class="alert alert-info">
                    <h5>Kupon Detayları:</h5>
                    <p><strong>Kullanıcı:</strong> @Model.User.FirstName @Model.User.LastName</p>
                    <p><strong>E-posta:</strong> @Model.User.Email</p>
                    <p><strong>Telefon:</strong> @Model.User.Phone</p>
                    <p><strong>Oluşturulma Tarihi:</strong> @Model.CreatedDate.ToString("dd/MM/yyyy HH:mm")</p>
                    <p><strong>Durum:</strong> 
                        @if (Model.IsUsed)
                        {
                            <span class="badge bg-danger">Kullanıldı</span>
                        }
                        else
                        {
                            <span class="badge bg-success">Aktif</span>
                        }
                    </p>
                </div>
            </div>
        </div>
        
        <div class="text-center mt-4">
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Yeni Kupon Oluştur</a>
        </div>
    </div>
</div>